version: '3.8'

services:
  postgres:
    image: bitnami/postgresql:latest
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=CiaAdmin
      - POSTGRES_PASSWORD=CiaAdmin1234
      - POSTGRES_DB=CiaDB
#  redis:
#    image: bitnami/redis:latest
#    ports:
#      - '6379:6379'
#    environment:
#      - REDIS_PASSWORD=CiaAdmin1234
